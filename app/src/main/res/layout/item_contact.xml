<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="16dp">

    <ImageView
        android:id="@+id/contact_profile_image_view"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_margin="12dp"
        android:scaleType="fitXY"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/car" />

    <TextView
        android:id="@+id/contact_name_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:textSize="22sp"
        app:layout_constraintBottom_toTopOf="@id/contact_phone_number_textview"
        app:layout_constraintStart_toEndOf="@id/contact_profile_image_view"
        app:layout_constraintTop_toTopOf="@id/contact_profile_image_view"
        tools:text="Ahmed Nabil" />

    <TextView
        android:id="@+id/contact_phone_number_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/contact_profile_image_view"
        app:layout_constraintStart_toEndOf="@id/contact_profile_image_view"
        app:layout_constraintTop_toBottomOf="@id/contact_name_textview"
        tools:text="0111111111" />
</androidx.constraintlayout.widget.ConstraintLayout>